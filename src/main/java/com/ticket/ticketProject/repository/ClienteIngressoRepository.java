package com.ticket.ticketProject.repository;

import com.ticket.ticketProject.model.ClienteIngresso;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClienteIngressoRepository extends JpaRepository<ClienteIngresso, Long> {
}
